=>project>Build>hositing>get stared
==========================
for first time and one time on your device
1. npm install -g firebase-tools(no need if you do it already!)(optional)
2. firebase login(if your project in your login gmail you don't have to)
3.firebase logout(optional) => Y the select the gmail account you want

=======================
for each project one-time
1.firebase init
2.Are you ready to proceed? (Y/n) =>  y
3.Which Firebase features do you want to set up for this directory? => Hosting: Configure files for Firebase Hosting and (optionally) set up GitHub Action deploys with (click down and up arrow and spacebar for confirm)
4.  Please select an option: Use an existing project => select what u neeed
5. What do you want to use as your public directory? (public) => dist(must write these)
6.Configure as a single-page app (rewrite all urls to /index.html)? (y/N)
7.Set up automatic builds and deploys with GitHub? (y/N) => n

=============================
for everytime diploy:

8.npm run build(optional)
9.firebase deploy
10. 
